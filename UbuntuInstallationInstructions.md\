// Install Ubuntu 18.04.5 LTS (Bionic Beaver). The version is important. These installation instructions won't work with the wrong version of Ubuntu.

// Using the Ubuntu terminal, copy and paste the following to install the required dependencies and to clone the Circcash repository.

sudo apt-get install git
 
sudo apt-get install build-essential libtool autotools-dev automake pkg-config libssl-dev libevent-dev bsdmainutils
 
sudo apt-get install libboost-system-dev libboost-filesystem-dev libboost-chrono-dev libboost-program-options-dev libboost-test-dev libboost-thread-dev
 
sudo apt-get install libboost-all-dev
 
sudo apt-get install software-properties-common
 
sudo add-apt-repository ppa:bitcoin/bitcoin
 
sudo apt-get update
 
sudo apt-get install libdb4.8-dev libdb4.8++-dev
 
sudo apt-get install libminiupnpc-dev
 
sudo apt-get install libzmq3-dev
 
sudo apt-get install libqt5gui5 libqt5core5a libqt5dbus5 qttools5-dev qttools5-dev-tools libprotobuf-dev protobuf-compiler 
 
sudo apt-get install libqt4-dev libprotobuf-dev protobuf-compiler

sudo apt-get install openssl1.0
 
sudo apt-get install libssl1.0-dev

git clone https://github.com/jvanname/circcash.git

// Navigate to the Circcash folder and type the following into the terminal to compile your Circcash wallet.

qmake

make

// Type the following to run Circcash and mine CIRCs.

./circcash-qt

// After you have opened your Circcash wallet, go to Help-Debug window-Console and then type "setgenerate true" in the textbox to start mining on your personal computer.

// Please send a message to Joseph Van Name at circcash9192020@protonmail.com to request further help.
